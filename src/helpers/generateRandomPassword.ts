export default function generateRandomPassword(length: number = 12): string {
    // TODO: Implement more randomness later
    const generatedPassword = `Temp${Date.now()}!Aa1`;
    return generatedPassword;
}