{
    "type": "object",
    "required": [
        "PORT",
        "POSTGRES_USERNAME",
        "POSTGRES_PASSWORD",
        "POSTGRES_URI",
        "POSTGRES_DB",
        "JWT_SECRET"
    ],
    "properties": {
        "PORT": {
            "type": "string",
            "default": 3000
        },
        "POSTGRES_USERNAME": {
            "type": "string",
            "default": "postgres"
        },
        "POSTGRES_PASSWORD": {
            "type": "string",
            "default": "postgres"
        },
        "POSTGRES_URI": {
            "type": "string",
            "default": "localhost"
        },
        "POSTGRES_DB": {
            "type": "string",
            "default": "capstone"
        },
        "JWT_SECRET": {
            "type": "string",
            "default": "your-secret-key-change-this-in-production"
        }
    }
}